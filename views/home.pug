extends layouts/main
include mixins/movieBlock

block content
    h1.movieTitle #{pagetitle}
    .movie
        each item in movies
            +movieBlock({
                title: item.title,
                id: item.id
            })
