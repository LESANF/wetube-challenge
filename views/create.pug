extends templates/layout

block content
    h1 Create
    form(action="/create", method="post")
        input(type="text", placeholder="Title" ,name="title", required=true)
        input(type="number", placeholder="Year", name="year", required=true)
        input(type="number", placeholder="Rating", name="rating", step="0.1" ,required=true)
        textarea(placeholder="Synopsis", name="synopsis", required=true)
        input(tpye="text", placeholder="Genres",name="genres", required=true)
        input(type="submit", value="submit")